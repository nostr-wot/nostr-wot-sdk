---
"nostr-wot-sdk": minor
---

Surface the new `@nostr-wot/graph` package: add `./graph` and `./graph/react` subpath re-exports (and a direct dependency on `@nostr-wot/graph`). Existing imports are unaffected.
